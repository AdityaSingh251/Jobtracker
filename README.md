# Job Tracker (Vercel Ready)

Backend: Express (Serverless on Vercel)
Frontend: React

## Local Run
### Backend
cd backend
npm install
npm run dev

### Frontend
cd frontend
npm install
npm start

## Deploy on Vercel
Add env vars: MONGO_URI, JWT_SECRET
